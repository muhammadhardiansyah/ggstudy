/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  // Mendukung output standalone atau export statis yang fleksibel untuk Vercel
  output: "standalone",
};

export default nextConfig;

